FUNKTION even (number){
    ALS number==0 DANN
    RETURN 1
    SLA
    number=number-1
    RETURN odd(number)
}

FUNKTION odd (number){
    ALS number == 0 DANN
    RETURN 0
    SLA
    number=number-1
    RETURN even(number)
}
